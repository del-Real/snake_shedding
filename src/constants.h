#pragma once

constexpr int SCREEN_WIDTH = 750;
constexpr int SCREEN_HEIGHT = 750;
constexpr int TILE_SIZE = 30;
constexpr int POINTS_EARNED = 100;
constexpr int MAX_TILES = (SCREEN_WIDTH / TILE_SIZE) - 1;
const int VERTICAL_LINES = SCREEN_HEIGHT / TILE_SIZE + 1;
const int HORIZONTAL_LINES = SCREEN_WIDTH / TILE_SIZE + 1;
